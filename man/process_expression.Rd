% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expression.R
\name{process_expression}
\alias{process_expression}
\title{Counts to log2-CPM, the training-pipeline version}
\usage{
process_expression(
  counts_mat,
  pheno_sub,
  target_col = NULL,
  gene_anns,
  gene_type_filter = c("protein_coding", "miRNA", "lncRNA"),
  min_count = 8,
  min_samples = 5,
  apply_filter = TRUE,
  use_log2 = TRUE
)
}
\arguments{
\item{counts_mat}{Genes (rows) x samples (cols) raw counts matrix.}

\item{pheno_sub}{Data frame with an \code{`RNA-seq`} column identifying
which columns of \code{counts_mat} to keep.}

\item{target_col}{Column of \code{pheno_sub} used as the \code{filterByExpr()}
grouping factor. Only required when \code{apply_filter = TRUE}.}

\item{gene_anns}{Data frame with \code{GENE_NAME}/\code{GENE_TYPE} columns.}

\item{gene_type_filter}{\code{GENE_TYPE} values to keep before normalization.}

\item{min_count, min_samples}{Passed to \code{edgeR::filterByExpr()} as
\code{min.count} and \code{min.prop = min_samples / ncol(sub_counts)}.}

\item{apply_filter}{Whether to apply \code{edgeR::filterByExpr()}. \code{TRUE} for
the training cohort, \code{FALSE} for a held-out cohort that must keep every
gene the training cohort kept.}

\item{use_log2}{Return log2-CPM (\code{edgeR::cpm(log = TRUE, prior.count = 1)})
instead of linear CPM.}
}
\value{
A genes x samples (log2) CPM matrix.
}
\description{
Restricts to a gene-type filter, matches samples against a phenotype
table, optionally applies \code{edgeR::filterByExpr()}, then computes (log2)
CPM with
\code{edgeR::cpm(normalized.lib.sizes = FALSE)}. This is the training-time
function — it needs \code{pheno_sub} to know which samples to keep and (when
\code{apply_filter = TRUE}) \code{target_col} to group by for \code{filterByExpr()}. For
scoring already-collected new data (no cohort/phenotype bookkeeping
needed), use \code{\link[=process_gene_expression]{process_gene_expression()}} instead.
}

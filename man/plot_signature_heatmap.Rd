% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/heatmap.R
\name{plot_signature_heatmap}
\alias{plot_signature_heatmap}
\title{Signature heatmap: final model genes across train + test patients}
\usage{
plot_signature_heatmap(
  fit,
  order_by = "nerve_obj_area_percent",
  split_cohort = FALSE
)
}
\arguments{
\item{fit}{A \code{gotnerve_fit} from \code{\link[=train_model]{train_model()}}.}

\item{order_by}{Column of \code{fit$pheno} to sort columns by (ascending),
or \code{NULL} to hierarchically cluster columns instead. Defaults to
\code{"nerve_obj_area_percent"}.}

\item{split_cohort}{If \code{TRUE}, split columns into separate Cohort A/B
blocks. Defaults to \code{FALSE} (one continuous block).}
}
\value{
A \code{ComplexHeatmap::Heatmap} object.
}
\description{
Row-scaled (z-score) log2-CPM for the genes with a non-zero coefficient
in the final model, with a left row annotation of each gene's final
glmnet coefficient and top annotations for cohort, clinical covariates,
true/predicted class, predicted probability, and total nerve area
percent — no other nerve-quantification annotation tracks are shown.
}

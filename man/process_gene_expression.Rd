% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expression.R
\name{process_gene_expression}
\alias{process_gene_expression}
\title{Process user-supplied expression data of a declared type}
\usage{
process_gene_expression(
  expr_data,
  input_type = c("counts", "cpm", "log2cpm"),
  gene_anns = NULL,
  gene_type_filter = c("protein_coding", "miRNA", "lncRNA")
)
}
\arguments{
\item{expr_data}{Genes (rows) x samples (cols) matrix or data frame.}

\item{input_type}{One of \code{"counts"} (raw counts; gene-type filtered and
converted with \code{edgeR::DGEList()}/\code{edgeR::cpm()}), \code{"cpm"} (already
library-size normalized on a linear scale; only log2-transformed, as
\code{log2(cpm + 1)}), or \code{"log2cpm"} (already fully processed; returned
unchanged).}

\item{gene_anns}{Data frame with \code{GENE_NAME}/\code{GENE_TYPE} columns. Only used
when \code{input_type = "counts"}; ignored (with a warning if genes can't be
filtered) for \code{"cpm"}/\code{"log2cpm"} since those are already normalized.}

\item{gene_type_filter}{\code{GENE_TYPE} values to keep when \code{input_type = "counts"}.}
}
\value{
A genes x samples log2-CPM matrix.
}
\description{
The general, prediction-time counterpart to \code{\link[=process_expression]{process_expression()}}: takes a
plain genes x samples matrix (or data frame) plus an explicit declaration
of what it already is, and returns log2-CPM without re-deriving anything
the caller has already done. No phenotype table, target column, or
\code{filterByExpr()} step is involved (those only make sense for the training
cohort's own gene selection).
}

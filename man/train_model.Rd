% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/train_model.R
\name{train_model}
\alias{train_model}
\title{Train the default gotNeRve elastic-net signature model}
\usage{
train_model(
  pheno_data,
  a_counts,
  b_counts,
  gene_anns,
  config = gotnerve_config()
)
}
\arguments{
\item{pheno_data}{Phenotype data frame (e.g. the bundled \code{pheno_data}).}

\item{a_counts, b_counts}{Cohort A / Cohort B genes x samples raw counts
matrices.}

\item{gene_anns}{Gene annotation data frame with \code{GENE_NAME}/\code{GENE_TYPE}.}

\item{config}{A config list from \code{\link[=gotnerve_config]{gotnerve_config()}}.}
}
\value{
A \code{gotnerve_fit} object (a list) with elements: \code{model} (the final
\code{caret::train} glmnet fit), \code{cv_model} (the out-of-fold tuning
predictions/results used for train-side ROC and KM plots),
\code{train_data}/\code{test_data} (model-ready data frames), \code{train_ids}/
\code{test_ids}, \code{target} (cleaned target name), \code{train_cohort}/\code{test_cohort},
\code{boot_summary} (stability-filter bootstrap audit trail), \code{train_cpm}/
\code{test_cpm} (log2-CPM matrices restricted to the top-variance gene set,
original gene names), \code{pheno} (combined, filtered train+test phenotype
data), and \code{test_predictions} (the held-out cohort's predicted class/
probability, obtained via the same scoring logic \code{\link[=gotNeRve]{gotNeRve()}} uses
internally, for full consistency with how any other new cohort is
scored).
}
\description{
Cohort split -> gene-type filter + CPM (via \code{\link[=process_expression]{process_expression()}}) ->
top-variance gene selection -> bootstrapped feature-stability filter ->
alpha/lambda tuning sweep -> final fixed-tune refit. Only the elastic-net
classifier path is implemented.
}
\section{RNG fidelity}{

To reproduce the exact default model, the
random-draw order below matters precisely, including one
call that looks dead but isn't: a \code{foldid} vector is drawn via \code{sample()}
before the stability filter's pre-CV \code{glmnet::cv.glmnet()} call but is
\emph{not} passed to it — it still consumes the RNG stream, so removing it
would change every downstream random draw.
}


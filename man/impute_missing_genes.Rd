% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{impute_missing_genes}
\alias{impute_missing_genes}
\title{Impute missing signature genes using training-cohort medians}
\usage{
impute_missing_genes(expr, sig_genes, reference = NULL)
}
\arguments{
\item{expr}{Genes (rows) x samples (cols) log2-CPM matrix, gene names
already run through \code{make.names()}.}

\item{sig_genes}{\code{make.names()}-ed signature gene names required by the
model.}

\item{reference}{Genes x samples log2-CPM matrix (original, non-\code{make.names()}
gene names) used to compute the per-gene medians. Defaults to the
package's bundled \code{train_log2cpm}.}
}
\value{
\code{expr} subset and reordered to exactly \code{sig_genes}, with any
missing rows filled in with that gene's median value in \code{reference}.
}
\description{
Used by \code{\link[=gotNeRve]{gotNeRve()}} when \code{new_data} is missing one of the model's
signature genes: fills it in with that gene's median log2-CPM value
across the training cohort, rather than assuming zero expression (which,
after centering/scaling on the training mean, would look like unusually
low expression instead of a neutral, typical value).
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{gotnerve_config}
\alias{gotnerve_config}
\title{Configuration for the gotNeRve default pipeline}
\usage{
gotnerve_config(
  target_var = "nerve_group",
  quantile_threshold = 0.33,
  train_cohort = "A",
  gene_type_filter = c("protein_coding", "miRNA", "lncRNA"),
  min_count = 8,
  min_samples = 5,
  top_n_var = 1000,
  ml_preproc = c("center", "scale"),
  cv_folds = 10,
  glm_alpha_step = 0.1,
  glm_stability = TRUE,
  glm_bootstraps = 1000,
  glm_stability_threshold = 0.25,
  glm_stability_sign_threshold = 0.5,
  glm_stability_alpha = 0.5,
  glm_stability_rule = "oneSE"
)
}
\arguments{
\item{target_var}{Phenotype column in \code{pheno_data} to predict.}

\item{quantile_threshold}{Extreme-quantile cutoff used to re-label
\verb{nerve_*} targets into High/Low, dropping the middle tier. Set to \code{NULL}
to skip this step for non-\verb{nerve_*} targets.}

\item{train_cohort}{Which cohort ("A" or "B") trains the model; the other
cohort becomes the held-out test set.}

\item{gene_type_filter}{\code{GENE_TYPE} values (exact match against
\code{gene_anns$GENE_TYPE}) to keep before normalization.}

\item{min_count, min_samples}{Passed to \code{edgeR::filterByExpr()}.}

\item{top_n_var}{Number of highest-variance training-cohort genes kept.}

\item{ml_preproc}{\code{caret::train()} \code{preProcess} methods for the final fit.}

\item{cv_folds}{Number of folds used by \code{glmnet::cv.glmnet()}.}

\item{glm_alpha_step}{Increment for the elastic-net alpha sweep, from 0 to
1 inclusive. The alpha/lambda combination is chosen by a 1-SE rule
applied across alpha: at each alpha, take its own \code{lambda.min} CV error;
find the global minimum of those; add its standard error to get a
threshold; among alphas at or below that threshold, keep the largest
(most-regularized/simplest) one, using its own \code{lambda.min}. (The
bootstrapped stability filter is separate and always uses
\code{lambda.1se}, via \code{glm_stability_rule}.)}

\item{glm_stability}{Whether to apply the bootstrapped feature-stability
filter before the alpha/lambda tuning sweep.}

\item{glm_bootstraps}{Number of stability-filter bootstrap resamples.}

\item{glm_stability_threshold}{Minimum bootstrap inclusion frequency for a
gene to survive the stability filter.}

\item{glm_stability_sign_threshold}{Minimum coefficient-sign consistency
(across bootstraps in which a gene was non-zero) required to survive.}

\item{glm_stability_alpha}{Fixed alpha used only during the stability
bootstrap (independent of \code{glm_alpha_range}).}

\item{glm_stability_rule}{\code{"oneSE"} or \code{"best"}, used to pick the fixed
lambda for the stability bootstrap.}
}
\value{
A named list.
}
\description{
Returns a named list of settings consumed by \code{\link[=train_model]{train_model()}}. Defaults
match the pipeline as originally run, with one intentional change:
\code{min_count = 8} (originally 10).
}
